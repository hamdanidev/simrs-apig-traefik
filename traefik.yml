api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":9080"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
  file:
    directory: /etc/traefik/dynamic
    watch: true # <--- Kunci agar perubahan otomatis muncul di dashboard
