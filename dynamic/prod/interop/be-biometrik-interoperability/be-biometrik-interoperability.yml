http:
  middlewares:
    simrs-cors:
      headers:
        accessControlAllowMethods:
          - "GET"
          - "POST"
          - "PUT"
          - "DELETE"
          - "OPTIONS"
        accessControlAllowHeaders:
          - "Authorization"
          - "Content-Type"
          - "X-Requested-With"
          - "X-Rs-Code"
        accessControlAllowOriginList:
          - "*"
        accessControlMaxAge: 100
        # Tambahkan baris di bawah ini untuk memastikan header Auth diizinkan
        addVaryHeader: true
  routers:

    # interop_biometrik_1:
    #   rule: "PathPrefix(`/v1.0/biometrik-face/compare/base64`) && (Method(`POST`) || Method(`OPTIONS`))"
    #   service: service-biometrik-interoperability
    #   entryPoints:
    #     - web
    #   middlewares:
    #     - "simrs-cors"
    # interop_biometrik_2:
    #   rule: "PathPrefix(`/v1.0/verification/verify`) && (Method(`POST`) || Method(`OPTIONS`))"
    #   service: service-biometrik-interoperability
    #   entryPoints:
    #     - web
    #   middlewares:
    #     - "simrs-cors"
    # interop_biometrik_3:
    #   rule: "PathPrefix(`/v1.0/biometrik-face/compare/filepath`) && (Method(`POST`) || Method(`OPTIONS`))"
    #   service: service-biometrik-interoperability
    #   entryPoints:
    #     - web
    #   middlewares:
    #     - "simrs-cors"

  services:
    # service-biometrik-interoperability:
    #   loadBalancer:
    #     servers:
    #       - url: "http://22.22.22.23:9112"
