http:
  routers:
    forensik_item_1:
      rule: "PathPrefix(`/v1.0/forensik/item-forensik/getlist`) && (Method(`GET`) || Method(`OPTIONS`))"
      service: service-forensik
      entryPoints:
        - web
      middlewares:
        - "simrs-cors"

  services:
    service-forensik:
      loadBalancer:
        servers:
          - url: "http://192.168.18.152:9085"
